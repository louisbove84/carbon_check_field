# Web deploy only: root pyproject + api/ triggers FastAPI detection on Vercel 50+.
# Flutter output is build/web (CI). Dart sources are not needed at the edge.
pyproject.toml
requirements-all.txt
pubspec.yaml
pubspec.lock
analysis_options.yaml
backend
ml_pipeline
lib
web
test
integration_test
venv
.venv
__pycache__
*.pyc
.dart_tool
android
ios
linux
macos
windows
docs
# Only root Flutter assets (bundled into build/web/assets by CI); do not match build/web/assets
/assets/

# Vercel CLI skips .gitignore paths — force-include Flutter output from GitHub Actions
!build
!build/web
!build/web/**
