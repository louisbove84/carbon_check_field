{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'Skip monorepo Python install on Vercel'",
  "buildCommand": "rm -rf public && mkdir -p public && cp -R build/web/. public/ && test -f public/index.html",
  "outputDirectory": "public",
  "ignoreCommand": "[ -d build/web ] && [ -f build/web/index.html ] || [ -f public/index.html ] || exit 1"
}
